let myBook = {
    title: '1984',
    author: 'George Orwell',
    pageCount: 326
}
console.log(`${myBook.title} by ${myBook.author}myBook.title`)

let Person = {
    name: 'Andraw',
    age: 27,
    address: 'Philadelphia'
}
console.log(`${Person.name} ${Person.age} ${Person.address}`)
Person.age = Person.age-1
console.log(`${Person.name} ${Person.age} ${Person.address}`)
